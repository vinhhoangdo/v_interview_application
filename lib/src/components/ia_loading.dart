import 'package:flutter/material.dart';

class IALoading extends StatelessWidget {
  const IALoading({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: CircularProgressIndicator(),
    );
  }
}
